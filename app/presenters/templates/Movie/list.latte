<!doctype html>
<meta charset="utf-8">
<title>Seznam filmů | Movies</title>
<h1>Seznam filmů</h1>

{var $count = 123} {* ha nulla az ertek, akkor nem jelenik meg a skladem*}
{$count ? skladem}

<ul>
	{foreach $movies as $movie}
		<li onclick="alert({$movie->name})"> {$movie['name']}({$movie['year']}</li>
	{*	<li title="{$movie->name}"> {$movie['name']}({$movie['year']}</li>*}
	{/foreach}
</ul>

<script>
	var name = {$movie->name};
</script>

<footer>Copyright 2018</footer>